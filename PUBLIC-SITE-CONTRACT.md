# 公开站点契约盘点

本文是 Wayfinder 前沿票「盘点公开站点契约」的规划资产。它回答“当前真正构成公开网站、因而需要进入 Next.js 迁移决策的事实是什么”，不决定最终子路由、组件拆分或验收实现。

## 证据基线

- 仓库基线：`main` 的 `b62b0184afc9d111de367b52d1932c7b6a3b2391`。
- 部署基线：`gh-pages` 的 `7659a4841e1e9d86d23a1cf0fbceca88e02db46c`，提交说明为从上述 `main` 提交同步。
- 复现方法：运行 `bash scripts/build-pages-site.sh <临时目录>`；本次构建通过 `scripts/check-public-prototype-shell.py`，并与 `gh-pages` 逐文件一致。
- 当前部署产物共有 89 个文件、18 个 HTML。迁移输入是其中 15 个有意公开的页面；其余 3 个 HTML 是不再保留的旧 URL 跳转，另有 37 个被目录递归复制意外带出的评审或研究文件。

“页面”在本文中的判断标准是：从公开首页经有意设置的站内链接可达，或由一个已链接应用明确输出为第二个 HTML 入口。仅仅能猜中静态文件地址，不会使文件成为站点页面。

## 结论摘要

1. 真正的站点契约是 1 个总首页、5 个项目模块、共 15 个页面。
2. 赛博数学当前没有中间项目入口：首页直接进入范·奥贝尔定理课件。
3. `排好菜/prototype-implementation/page-map.html` 虽然是页面地图并包含“计划中”状态，但它由 Vite 显式构建、由运行原型直接链接，属于有意公开页面，不是意外评审材料。
4. 站点没有 hash 驱动的页面状态；唯一的 `href="#recycle-icon"` 是页面内 SVG 符号引用。
5. 只有两个 `localStorage` 契约：街坊味的配送小区跨两个页面共享；排好菜工程原型在自身页面内持久化家庭设置、菜品池、菜单和历史。
6. 所有页面都不请求业务 API；没有 `fetch`、WebSocket、cookie、`sessionStorage`、Service Worker、后端或真实文件上传依赖。
7. 当前构建脚本按扩展名递归复制五个中文目录，导致 37 个非站点文件被发布；Next.js 构建只能发布站点模块自身的静态导出，不能复制仓库其他目录。

## 页面与入口关系

五个一级英文路由已经由任务地图固定。下面只把它们映射到当前页面身份；每个子页面的最终英文路径留给「锁定完整规范路由树」决定。

```text
总首页 /                                                    [1]
├─ /community-mutual-aid-team/ 近邻互助组入口               [2]
│  ├─ 客户体验原型                                          [3]
│  └─ 实施对照原型                                          [4]
├─ /kith-inn/ 街坊味入口                                    [5]
│  ├─ 客户体验原型                                          [6]
│  └─ 实施对照总蓝图                                        [7]
│     ├─ 顾客使用端原型                                     [8]
│     └─ 桃子使用端原型                                     [9]
├─ /stairwell-care/ 楼道收一收                              [10]
├─ /weekly-meal-planner/ 排好菜入口                         [11]
│  ├─ 客户体验原型                                         [12]
│  └─ 可运行工程原型                                       [13]
│     └─ 手机页面地图                                      [14]
└─ /cyber-math/ 范·奥贝尔定理互动课件                      [15]
```

当前源码和入口关系如下。

| 页码 | 页面身份 | 当前源码/构建路径 | 主要站内入口与返回关系 |
| --- | --- | --- | --- |
| 1 | 总首页 | `index.html` | 进入五个项目；近邻互助组、街坊味、排好菜、楼道收一收进入各自入口，赛博数学直接进入课件 |
| 2 | 近邻互助组入口 | `牛马互助平台/index.html` | 返回总首页；进入客户体验或实施对照 |
| 3 | 近邻互助组客户体验 | `牛马互助平台/prototype-customer/index.html` | 面包屑和手机工具栏返回项目入口/总首页 |
| 4 | 近邻互助组实施对照 | `牛马互助平台/prototype-implementation/index.html` | 面包屑返回项目入口/总首页；页面中的实施依据链接到 GitHub 上的 User Stories 和原型边界文档 |
| 5 | 街坊味入口 | `街坊味/index.html` | 返回总首页；进入客户体验或实施对照总蓝图 |
| 6 | 街坊味客户体验 | `街坊味/prototype-customer/index.html` | 面包屑和手机工具栏返回项目入口/总首页 |
| 7 | 街坊味实施对照总蓝图 | `街坊味/prototype-implementation/index.html` | 返回项目入口/总首页；分别以 `step` 深链进入桃子端和顾客端 |
| 8 | 街坊味顾客使用端 | `街坊味/prototype-implementation/prototype-customer/index.html` | 返回实施对照总蓝图、项目入口或总首页 |
| 9 | 街坊味桃子使用端 | `街坊味/prototype-implementation/prototype-taozi/index.html` | 返回实施对照总蓝图、项目入口或总首页 |
| 10 | 楼道收一收 | `楼道回收提醒/index.html` | 返回总首页；同一文档内切换住户端和师傅端 |
| 11 | 排好菜入口 | `排好菜/index.html` | 返回总首页；进入客户体验或可运行工程原型 |
| 12 | 排好菜客户体验 | `排好菜/prototype-customer/` 的 Vite 输出 | 返回项目入口/总首页；可切到工程原型 |
| 13 | 排好菜可运行工程原型 | `排好菜/prototype-implementation/` 的 Vite 输出 | 返回项目入口；进入手机页面地图；以 `step=generate` 进入客户体验 |
| 14 | 排好菜手机页面地图 | `排好菜/prototype-implementation/page-map.html` 的 Vite 输出 | 返回项目入口或工程原型；以 `step=generate` 进入客户体验 |
| 15 | 范·奥贝尔定理互动课件 | `赛博数学/van-aubel-courseware.html` | 当前没有站内返回链接；浏览器返回是唯一回到总首页的路径 |

总首页和使用公共壳层的页面还会链接到 `https://www.codeforpeople.cn/`；这是一条站外品牌链接，不是站点路由。

## 页面契约

### 总首页与静态入口页

- 总首页静态展示近邻互助组应用图谱和独立的赛博数学探索线。五个可点击入口必须保持可达；“待探索”圆形没有链接。
- 近邻互助组入口、街坊味入口和排好菜入口都是二选一的原型入口页，本身没有业务状态。
- 街坊味实施对照总蓝图是二级入口页，四个 CTA 分别打开桃子端的 `intro`/`orders` 和顾客端的 `entry`/`orders`。
- 公共壳层脚本在总首页、近邻互助组 3 页、街坊味 5 页和楼道收一收共 10 页上插入原型免责声明页脚。
- 近邻互助组客户体验、街坊味客户体验、街坊味顾客端、街坊味桃子端始终提供移动端退出工具栏。楼道收一收只有在 `prototype=1` 时启用该工具栏。
- 排好菜两套 Vite 应用使用自己的移动端退出工具栏，不依赖公共壳层。

### 近邻互助组客户体验

- 五步讲解：`intro`（价值主张）→ `need`（外部求助）→ `trust`（亲历证据）→ `chat`（混合会话）→ `finish`（产品全貌），首尾循环。
- CTA、手机内关键卡片和底部前后按钮都会切换步骤；键盘左右方向键也会切换，输入框聚焦时除外。
- `chat` 步骤进入后自动把消息线程滚到底部；收件人按钮只切换视觉选中状态，输入和发送不产生消息。
- 步骤以 query 驱动，可直达和刷新；其余状态只存在于页面内存。

### 近邻互助组实施对照

- 两条场景：
  - `lu`：收到服务记录 → 确认服务记录 → 消息列表首页 → 发现服务者 → 提供服务 → 我的亲历卡。
  - `taozi`：微信群外部求助 → 亲历先行者分享 → 亲历推荐卡回到群里 → 求助者查看亲历。
- 场景和主屏可由 query 直达。页内可用场景按钮、旅程节点、前后按钮和左右方向键导航。
- 关键内存交互包括：确认服务记录并选择公开/私密、选择并分享亲历推荐卡、填写服务意向、展开月卡价格、在混合会话中切换真人/Agent 收件人并发送本地消息、建立新需求对话、搜索另一服务者、切换会话、语音亲历草稿与分享。
- “与老张建立四方混合会话”和非桃子场景的转发使用浏览器 `alert()` 模拟，没有真实跳转或数据写入。
- 页面运行时展示的 User Story 与原型边界链接指向 GitHub；这些 Markdown 文档不是 ideal 站点页面或运行资产。
- 除 query 外没有持久化；刷新会保留场景和主屏，但会丢失屏内展开、输入和会话状态。

### 街坊味客户体验

- 六步讲解：`generate` → `menu` → `overview` → `share` → `orders` → `reconcile`，首尾循环。
- 生成 CTA 进入菜单；菜单支持横向拖动、荤菜/全部筛选、选中单菜、前三次智能换菜、随后打开同类候选列表；确认后进入只读总览。
- 订单步骤支持午饭/晚饭/全部筛选；对账步骤支持日期分组折叠、匹配依据展开和手动勾销/恢复。
- 底部按钮和键盘左右方向键切换讲解步骤；拖动菜单时抑制紧随其后的误点击。
- 所有菜单、筛选、替换和对账变化只在内存中存在；刷新只从 query 恢复讲解步骤。

### 街坊味顾客使用端

- 五步：`entry`（微信群开饭卡）→ `choose`（短单/长期单）→ `short` → `long` → `orders`。
- 可展开午饭/晚饭的完整菜品；短单和长期单都支持餐次选择、1—20 份数量校验、配送地址编辑、提交前摘要、提交完成状态和返回修改。
- 长期单额外校验 4—90 天；订单页可确认已收到停餐退款；短单可生成供微信发送的摘要，但不调用分享 API。
- 地址小区只能从桃子端保存的配送范围中选择；楼栋门牌只在本页面内存中修改，不写入持久化。
- 页面监听 `storage` 事件；另一个页面或标签页更新配送小区时，会刷新可选项并纠正已不在范围内的当前小区。
- 底部讲解按钮和键盘左右方向键切换步骤；输入或下拉框聚焦时不响应方向键。

### 街坊味桃子使用端

- 九步：`intro` → `settings` → `dishes` → `delivery` → `plan` → `publish` → `orders` → `reconcile` → `finish`。
- 菜品池支持多行粘贴、按名称推断荤/素/汤、循环纠正分类并确认；这些菜品变化只在内存中存在。
- 配送范围支持新增、去重、移除并要求至少保留一个小区；确认时写入跨页 `localStorage`。
- 菜单支持生成五天十餐、展开日期、同类轮换、第三次后打开候选列表、确认整周、发布并锁定明日菜单，以及开饭/停餐公告状态。
- 订单支持餐次筛选、长期订单批准/生效/拒绝理由、微信文案状态，以及手工代录订单和字段校验。
- 对账支持模拟扫描收款截图、系统唯一匹配和手动勾销；没有真实文件读取或 OCR。
- 底部应用导航、页面返回、讲解前后按钮和键盘方向键共同驱动页面；分类/换菜图标用 300ms 定时器完成旋转反馈。
- 除配送小区外，所有状态只在内存中存在。

### 楼道收一收

- 单页内共享一份内存任务列表，在住户视图与师傅视图之间切换。初始师傅端有 12、8、3 楼三组演示任务。
- 住户端自动识别“幸福苑 2 栋 · 6 楼”，选择回收或打扫后用 850ms 定时器模拟上报；可撤回、补充示例照片和最多 80 字留言。
- 住户成功回执有 A/B/C 三个可切换方案：轻补充、极简回执、进度回执。左右方向键切换方案。
- 师傅端按楼层显示任务；点击楼层打开操作抽屉，可标记完成或“现场没发现”，并可撤销。完成 6 楼回收会反映到住户回执；页面提供恢复演示数据。
- 操作抽屉可点遮罩/关闭按钮、按 Escape 或向下滑超过 64px 关闭；重渲染会保留师傅列表滚动位置。
- 页面监听 `popstate` 并重读视图/方案 query。除 query 外，任务、照片、留言和完成状态刷新即丢失。
- 源文件仍保留一套旧的 `residentStep` 表单函数和动作标记，但当前 `renderResident()` 与事件处理器不可达这些函数；它们不是公开行为契约，不应作为迁移功能复制。

### 排好菜客户体验

- 五步：`generate` → `menu` → `swap` → `overview` → `saved`。
- 支持三周切换、菜单横向原生滚动、荤菜/全部筛选、选择单菜、从同类候选替换、只读总览、保存结果、展开历史详情和重新体验。
- 步骤导航使用 `history.pushState()`，并监听 `popstate`，因此浏览器前进/后退会恢复步骤。
- 周、选中菜、替换结果、筛选和历史详情只存在于 React 内存；刷新只恢复 `step`。

### 排好菜可运行工程原型

- 两种演示入口：新用户首次准备和已完成准备的回访用户。
- 首次准备有六步：欢迎、家庭设置、每餐结构、左右滑动挑选常吃菜、补充并分类自家菜、完整度检查。家庭人数限制 1—20；至少保留一个餐次和一天；自定义每餐结构有数量上限。
- 主应用有四个底部入口：排菜单、菜品库、历史、我的。它们共享家庭规则、菜品池和三周菜单状态。
- 排菜单支持周切换、按菜品池生成、荤菜/全部视图、单菜智能候选或菜品库手选、确认、保存；生成有 620ms 模拟等待。
- 菜品库支持搜索、分类筛选、详情、未分类自家菜归类、手工添加和看图片左右滑动继续挑菜。
- 历史支持只在已保存周之间切换、完整只读菜单、复制整周到下一周并继续调整。
- “我的”中的关于、联系、协议、隐私和数据说明都以本页弹层打开；弹层、复制确认框和添加菜抽屉打开时锁定文档滚动。
- toast 可点击关闭，也会在 2100ms 后自动关闭。左右滑动菜卡同时支持 Pointer Events 和 HTML Drag Events。
- URL 只覆盖演示模式、顶层 tab、菜单屏、周和首次准备步骤。页面加载时读取一次，之后用 `history.replaceState()` 同步；当前没有 `popstate` 监听，因此浏览器前进/后退不会反向驱动已挂载的 React 状态。
- `demo=new` 是明确的全新开始边界：忽略已有本地数据且不写入；首次准备完成或 `demo=ready` 后恢复并持久化完整演示数据。

### 排好菜手机页面地图

- 公开展示五条页面旅程：首次准备、本周排菜、复用上周、规则变更、补充验证；其中明确区分已实现、计划中和补充验证状态。
- 支持按旅程筛选、点击/键盘聚焦单个手机节点、前后节点跳转、画布拖动、滚轮缩放、放大/缩小/适配，以及窗口缩放后重新适配。
- 页面初始化时把手机模型里的按钮和输入替换成静态展示控件；它展示状态关系，不执行运行原型的表单行为。
- 无 query、hash 或持久化状态。

### 范·奥贝尔定理互动课件

- 八页教学路径：原题全貌、倒推全等、旋转终局、为什么是 M、一分为二、三角形模型、动画复盘、路线总结。
- 左侧步骤、底部圆点、上一页/下一页和左右方向键均可导航；最后一页“下一页”重新开始，但键盘右键停在最后一页。
- 教师提示可展开/收起；切页时自动收起。
- 普通页使用 Canvas 2D 绘制几何图；动画页播放内嵌 GIF。`ResizeObserver` 在容器尺寸变化后重绘。
- 支持 `window.print()` 和 Fullscreen API；画布区域左右触摸滑动超过 55px 时切页。
- 无 query、hash 或持久化状态。

## Query、history 与 hash 清单

| 页面 | 参数 | 有效值与默认值 | 写入和历史行为 |
| --- | --- | --- | --- |
| 公共壳层 | `prototype` | `1` 仅对楼道收一收的手动手机模式有效 | 壳层只读取，不写入 |
| 近邻互助组客户体验 | `step` | `intro`、`need`、`trust`、`chat`、`finish`；默认 `intro` | `replaceState`；`popstate` 重渲染 |
| 近邻互助组实施对照 | `scenario` | `taozi` 或默认 `lu` | 初次载入并在场景按钮中写入；`popstate` 不重新读取该字段 |
| 近邻互助组实施对照 | `variant` | `lu` 场景为 `private`、`confirm`、`home`、`discover`、`provide`、`mycards`；`taozi` 场景为 `wechat`、`pioneer`、`shared`、`requester`；默认各场景首项 | 每次渲染读取，页内导航用 `replaceState`；`popstate` 重渲染 |
| 街坊味客户体验 | `step` | 六个主步骤；兼容别名 `order`→`orders`、`ledger`→`reconcile`；默认 `generate` | `replaceState`；`popstate` 重渲染 |
| 街坊味顾客端 | `step` | `entry`、`choose`、`short`、`long`、`orders`；默认 `entry` | `replaceState`；`popstate` 重渲染 |
| 街坊味桃子端 | `step` | 九个主步骤；兼容别名 `generate`→`plan`、`adjust`→`publish`；默认 `intro` | `replaceState`；`popstate` 重渲染 |
| 楼道收一收 | `view` | `resident` 或默认 `collector` | 视图变化用 `replaceState`；`popstate` 重读 |
| 楼道收一收 | `variant` | `A`、`B`、`C`；默认 `A` | 方案变化用 `replaceState`；`popstate` 重读 |
| 排好菜客户体验 | `step` | `generate`、`menu`、`swap`、`overview`、`saved`；默认 `generate` | `pushState`；`popstate` 重读 |
| 排好菜工程原型 | `demo` | 只有 `ready` 进入回访用户，其余值进入 `new`；运行后规范化为 `ready`/`new` | 初次读取，之后 `replaceState`；无 `popstate` 监听 |
| 排好菜工程原型 | `tab` | `schedule`、`library`、`history`、`profile`；默认 `schedule` | 初次读取，之后 `replaceState`；无 `popstate` 监听 |
| 排好菜工程原型 | `screen` | `home`、`edit`、`swap`、`review`；默认 `home`，无菜单数据时强制 `home` | 初次读取，之后 `replaceState`；无 `popstate` 监听 |
| 排好菜工程原型 | `week` | `0`、`1`、`2`；默认 `1` | 初次读取，之后 `replaceState`；无 `popstate` 监听 |
| 排好菜工程原型 | `onboarding` | 新用户时 `0`—`5`；默认 `0`；回访用户会删除该参数 | 初次读取，之后 `replaceState`；无 `popstate` 监听 |

没有公开页面读取或写入 `location.hash`。三个旧 URL 跳转会原样转送 query 和 hash，但这些跳转已被任务地图明确排除，不是新站契约。

## localStorage 与跨页状态

| key | 数据形态 | 写入方 | 读取方 | 契约边界 |
| --- | --- | --- | --- | --- |
| `kith-inn-delivery-communities` | 非空字符串数组；默认 `桂花苑`、`梧桐里`、`春和家园` | 街坊味桃子端确认配送范围时 | 桃子端初始化；顾客端初始化并监听 `storage` 事件 | 街坊味模块内唯一跨页面状态；依赖同源 `localStorage` |
| `paihaocai.prototype.dish-pool.v1` | 当前写入 `version: 2`：家庭设置、餐型、自定义餐型、已选/自建菜、三周菜单、当前周、历史周；兼容读取 `version: 1` 旧菜品池 | 排好菜工程原型的回访用户状态变化时 | 同一页面以 `demo=ready` 载入时 | 只属于排好菜工程原型；载入时会校验、限幅、去重和兼容旧版；`demo=new` 忽略且暂停写入 |

除上表外，不存在跨页状态。特别是：

- 街坊味顾客的楼栋门牌、订单和退款确认不会写入本地存储。
- 排好菜客户体验与工程原型不共享状态。
- 楼道收一收的住户/师傅状态只在同一文档内共享。
- 近邻互助组两套原型之间不共享状态。

## 浏览器事件与客户端接缝

| 能力 | 使用页面 | 必须知道的现状 |
| --- | --- | --- |
| DOM 注入 | 公共壳层 10 页 | 加载时插入免责声明页脚和条件式手机工具栏 |
| `click`/表单输入 | 所有交互原型 | 主要交互入口；多数单文件原型用文档级事件委托 |
| `popstate` | 近邻客户体验、近邻实施对照、街坊味客户体验、街坊味顾客端、街坊味桃子端、楼道收一收、排好菜客户体验 | 排好菜工程原型是唯一有 URL 状态但不监听 `popstate` 的页面 |
| `storage` | 街坊味顾客端 | 跨页面/标签页同步配送小区 |
| Pointer Events | 街坊味客户体验、排好菜工程原型、排好菜页面地图 | 菜单拖动、菜卡滑动、画布拖动/缩放 |
| HTML Drag Events | 排好菜工程原型 | 作为菜卡左右滑动的第二输入路径 |
| Touch Events | 楼道收一收、赛博数学 | 关闭师傅抽屉；课件左右滑页 |
| 键盘 | 近邻两页、街坊味三套交互页、楼道收一收、排好菜页面地图、赛博数学 | 方向键切步骤/方案；Escape 关抽屉；页面地图节点可用 Enter/Space 聚焦 |
| 定时器 | 楼道收一收、街坊味桃子端、排好菜工程原型 | 模拟上报/生成和短暂旋转/toast 反馈 |
| Canvas 2D + `ResizeObserver` | 赛博数学 | 几何图按容器尺寸重绘 |
| Print + Fullscreen API | 赛博数学 | 打印当前课件和全屏讲解 |
| 文档滚动锁 | 排好菜工程原型 | 弹层、抽屉和确认框打开时锁定 `body` 与根元素 |

## 运行所需资产

以下是页面运行或视觉等价所需的资产；当前构建生成的哈希 JS/CSS 是产物，不应被当作迁移源文件复制。

| 归属 | 资产 | 用途 |
| --- | --- | --- |
| 站点公共壳层 | `prototype-shell.css`、`prototype-shell.js` | 10 个公共壳层页面的品牌头、状态条、面包屑、免责声明页脚和手机工具栏 |
| 站点公共壳层 | `https://www.codeforpeople.cn/assets/brand/code-for-people-logo.png` | 10 个公共壳层页面的远程品牌图；是当前唯一站外运行图片依赖 |
| 排好菜入口 | `prototype-nav.css` | 排好菜二选一入口页样式 |
| 街坊味 | `街坊味/prototype-implementation/kith-inn-logo.png` | 街坊味入口、顾客端和桃子端品牌图 |
| 街坊味桃子端 | `街坊味/prototype-implementation/prototype-taozi/refresh-cw.svg` | 菜品分类和换菜旋转按钮 |
| 楼道收一收 | `楼道回收提醒/assets/stairwell-recyclables.png` | 住户补充现场照片的示例图 |
| 排好菜两套 Vite 应用 | 各自的 `assets/iphone/Bezel.png`、`assets/status/ios-status-icons.svg` | 手机边框和状态栏；两套副本逐字节相同 |
| 排好菜工程原型 | `public/assets/dishes/*.jpg` 共 18 张 | 首次挑菜、菜品库和推荐挑菜的菜图 |
| 赛博数学 | HTML 中内嵌的 GIF（解码后 1,154,442 字节） | 第 7 页证明动画 |
| 赛博数学 | 页面内 Canvas 绘图代码 | 其余七页几何示意，不依赖外部图片 |
| 当前分支式 Pages 部署 | `CNAME`、`.nojekyll` | 部署承载文件，不是页面运行资产；最终去留由「选择 GitHub Pages 构建与原子切换设计」决定 |

React、React DOM、Vite 与 Radix Icons 是当前两套排好菜构建依赖，不是需要复制到 `public/` 的静态文件。未来 Next.js 依赖与资源归属由模块拆分方案决定。

## 明确排除

### 当前已经意外出现在部署产物中的文件

| 当前公开路径模式 | 数量 | 处理结论 |
| --- | ---: | --- |
| `排好菜/docs/review/**` | 11 张图片 | 评审参考、对比和截图；不迁移、不发布 |
| `楼道回收提醒/research/assets/**` | 5 张图片 | 第三方产品研究截图；不迁移、不发布 |
| `赛博数学/research/**/*.json` | 21 个 JSON | 模型验证摘要、用量和运行元数据；不迁移、不发布 |
| `牛马互助平台/消费者联盟-亲历推荐-prototype.html` | 1 个 HTML | 旧入口跳转；不保留 |
| `牛马互助平台/近邻互助组-客户体验-prototype.html` | 1 个 HTML | 旧入口跳转；不保留 |
| `牛马互助平台/近邻互助组-实施对照-prototype.html` | 1 个 HTML | 旧入口跳转；不保留 |

前 37 个文件是构建扫描泄漏，不是页面、入口、测试基线或迁移资产。三个跳转虽然是有意生成的兼容页，但任务地图已经决定不保留中文旧路由和 `.html` URL。

### 即使当前没有被构建复制，也始终属于非站点材料

- 根目录和各项目的 `design-qa.md`、`design-qa-screenshots/**`、`docs/review/**`、`qa/**`、对比图和评审截图。
- 所有 `research/**`，包括截图、视频清单、模型输出、盲评映射、用量和运行元数据。
- 所有 `draft/**`、`drafts/**`、草案、临时工程板和未采用方案。
- 所有 `会议纪要/**`、准备记录、审计报告、产品决议、User Stories、PRD、需求梳理、资料索引和 README；它们可以继续作为仓库知识来源，但不成为 ideal 站点页面或静态资产。
- `初期头脑风暴/`、`第一个产品/`、`小金毛辩论赛摆摊/`、`随口记/` 及其印刷品、二维码、PDF、DOCX 和历史 HTML。
- 排好菜两个 package 内的源码测试、模型文档、工程计划、审计、草稿、QA 页面和截图。只有应用构建出的有意公开页面与上节列出的运行资产进入迁移输入。

近邻互助组实施对照页面目前链接 GitHub 上的 User Stories 和原型边界文档；保留这条站外链接行为不等于把这些文档复制进新站。

## 后续决策的输入

- 「锁定完整规范路由树」必须为上述 15 个页面身份分配最终路径，并明确是否保留现有 query 别名；不能新增旧中文路径或 `.html` 兼容入口。
- 「锁定交互状态与浏览器契约」必须逐项裁定本文记录的 URL、history、`localStorage`、`storage`、Pointer/Drag/Touch/Keyboard、Canvas、打印和全屏行为是否原样保留，并处理已记录的 `popstate` 不对称。
- 「Review 页面与模块拆分方案」应把两个跨页持久化接缝放在各自项目模块内；公共壳层和逐字节相同的排好菜设备资产是可评估复用点，但不能因此把项目状态耦合起来。
- 「定义迁移等价证据与发布门禁」应以 15 页路由矩阵、本文的入口边、可直达 query 状态、两项本地存储契约和浏览器事件表作为测试种子；楼道收一收的不可达旧函数不进入等价基线。
- 巨型单文件原型的具体风险已经清晰：楼道收一收有不可达旧流程，赛博数学有 1.15MB 内嵌动画，排好菜页面地图混合已实现/计划中状态。这些分别由模块拆分和等价门禁票覆盖，无需另建重复决策票。

## 本票验收核对

- [x] 页面身份与入口关系覆盖总首页和五个项目模块。
- [x] 运行所需静态资产、远程资产和部署文件已区分。
- [x] query、history、hash、`localStorage`、跨页状态和浏览器事件已盘点。
- [x] 关键交互与刷新后的状态边界已记录。
- [x] 当前构建意外公开的 37 个材料文件和 3 个淘汰跳转已明确排除。
- [x] 非站点知识、研究、草稿、会议纪要、评审材料和截图已形成长期排除规则。
- [x] 未提前决定子路由、组件拆分、部署方案或验收实现。
